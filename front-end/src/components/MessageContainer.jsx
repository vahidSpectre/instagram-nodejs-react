import React from 'react';

import classes from './MessagesContainer.module.css';
const MessageContainer = () => {
  return <div className={classes.main}>MessageContainer</div>;
};

export default MessageContainer;
