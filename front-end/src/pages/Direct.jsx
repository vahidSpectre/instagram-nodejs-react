import React from 'react';

import sharedStyles from '../styles/layout.module.css';
import classes from './Direct.module.css';
const Direct = () => {
  return <div className={`${classes.main} ${sharedStyles.shared}`}>Direct</div>;
};

export default Direct;
