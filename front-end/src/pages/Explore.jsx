import React from 'react';

import sharedStyles from '../styles/layout.module.css';
import classes from './Explore.module.css';
const Explore = () => {
  return (
    <div className={`${classes.main} ${sharedStyles.shared}`}>Explore</div>
  );
};

export default Explore;
